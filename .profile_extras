# Insert a reference to this in your distro's default .profile
# e.g.
#
# if [ -f "${HOME}/pfcfg/.profile_extras" ] ; then
#     . ${HOME}/pfcfg/.profile_extras
# fi

# Set Git information in the shell prompt:
PS1='${debian_chroot:+($debian_chroot)}\[\033[00;32m\]\u@\h:\[\033[01;34m\]\w\[\033[00m\]$(__git_ps1 "(%s)")\[\033[00;32m\]\n\$\[\033[00m\] '

# Use the KDE session's ssh agent
export SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/gcr/ssh

# Use the rootless Docker socket
export DOCKER_HOST=unix://${XDG_RUNTIME_DIR}/docker.sock
