
# If the tab count == 1 then stay blank (only show the default window title)
# If there is more than one tab
#   If the window is named show the window name
#   Show the tab count

%IfRegexMatch(%TABCOUNT%,^1$,gi,,(%IfWindowName(%WindowName% |,)%ʭ%TabCount%) )%
