# Execute this file via tm.

# Test session for testing:
SESSION=$1

# Configurable variables:
BASEDIR=$HOME/.tmux

# Window 1 - vim and entr-based compile, plus a tiny Vagrant ssh
# Left pane - vim
tmux new-window -c $BASEDIR -n VIM -t $SESSION:1
tmux send-keys -t $SESSION:1.1 vim C-m

# Right pane - entr
tmux split-window -d -c $BASEDIR -t $SESSION:1 -h -l 80
tmux send-keys -t $SESSION:1.2 "watch ls -la" C-m

# Bottom right pane - vagrant ssh for restarting server
tmux split-window -d -c $BASEDIR -t $SESSION:1.2 -v -l 5
tmux send-keys -t $SESSION:1.3 "ssh paul@pi" C-m

# Window 2 - bash for git
tmux new-window -d -c $BASEDIR -n GIT -t $SESSION:2

# Window 3 - test repo
tmux new-window -d -c $HOME/src -n TESTREPO -t $SESSION:3

